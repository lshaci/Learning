# server
server:
  port: 9001
# spring
spring:
  # application
  application:
    name: provider
  # cloud
  cloud:
    # nacos
    nacos:
      server-addr: localhost:8848
# logging
logging:
  config: classpath:config/logback-spring.xml
  level:
    root: warn
    com.lshaci: info
